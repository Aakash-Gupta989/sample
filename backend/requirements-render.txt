# Ultra-lightweight requirements for Render free tier
fastapi
uvicorn[standard]
websockets
python-multipart
python-dotenv
python-socketio
aiofiles
httpx
openai
groq
requests

# Note: Removed heavy packages that cause build issues
# - pydantic (causes Rust compilation issues)
# - httptools (causes compilation issues) 
# - Pillow (causes compilation issues)
# - numpy (causes compilation issues)
# These features will be disabled in production
